---
- name: Install HTTP server 
  hosts: web_servers
  become: yes
  tasks:
    - name: Install Apache2 
      apt:
        name: Apache2
        state: present

    - name: Start and enable apache2
      service:
        name: apache2
        state: started
        enabled: yes

...        